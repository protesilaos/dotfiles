Thinkpad X220 backlight in Debian Buster/Sid
============================================

This allows to change screen brightness using `xbacklight`.

IMPORTANT. This directory should not be used with `stow`.

Copy the `xorg.conf` to /etc/X11/

To get the device identifier, run

    xrandr --verbose | grep Identifier

It should be the first line.
